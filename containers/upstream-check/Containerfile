FROM cgr.dev/chainguard/wolfi-base:latest

RUN apk add --no-cache \
    curl \
    jq \
    github-cli \
    coreutils

COPY scripts/upstream-check.sh /usr/local/bin/upstream-check
COPY UPSTREAM.md /work/UPSTREAM.md

WORKDIR /work

ENTRYPOINT ["upstream-check"]
